include(
        "movie",
        "shared",
        "actor",
        "clients",
        "eureka-server",
        "config-server"
)

rootProject.name = 'micro-movie'

rootProject.children.forEach{
    it.buildFileName = "${it.name}.gradle"
}